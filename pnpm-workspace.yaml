allowBuilds:
  esbuild: true
  unrs-resolver: true
  # Optional native addons for `ws` (perf only) and a core-js postinstall
  # notice — nothing here needs to compile.
  bufferutil: false
  core-js: false
  utf-8-validate: false
